apiVersion: v1
kind: Service
metadata:
  name: signalrank
spec:
  selector:
    app: signalrank
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8000
  type: {{ .Values.service.type }}
